{
  "name": "rifq-app",
  "version": "6.4.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "android:add": "npm run mushaf:download && npm run mushaf:check && npm run offline:download && npm run offline:check && node scripts/ensure-android-platform.mjs && npm run build:android && npm run android:assets && npx cap sync android && npm run android:copy-mushaf && node scripts/check-android-mushaf-assets.mjs && node scripts/check-android-offline-assets.mjs",
    "android:sync": "npm run android:assets && npx cap sync android && npm run android:copy-mushaf && node scripts/check-android-mushaf-assets.mjs && node scripts/check-android-offline-assets.mjs",
    "android:open": "npx cap open android",
    "android:apk": "npm run android:prepare && cd android && gradlew.bat assembleDebug",
    "android:assets": "npx capacitor-assets generate --android",
    "mushaf:download": "node scripts/download-mushaf-pages.mjs",
    "mushaf:check": "node scripts/check-mushaf-pages.mjs --strict",
    "android:prepare": "npm run mushaf:download && npm run mushaf:check && npm run offline:download && npm run offline:check && node scripts/ensure-android-platform.mjs && npm run build:android && npm run android:assets && npx cap sync android && npm run android:copy-mushaf && node scripts/check-android-mushaf-assets.mjs && node scripts/check-android-offline-assets.mjs",
    "android:build-debug": "cd android && gradlew.bat clean assembleDebug",
    "android:copy-mushaf": "node scripts/copy-mushaf-to-android-assets.mjs",
    "build:android": "node scripts/build-android-web.mjs",
    "mushaf:status": "node scripts/check-mushaf-pages.mjs",
    "android:init": "npm run android:add",
    "quran-text:download": "node scripts/download-offline-quran-pages.mjs",
    "tafsir:download": "node scripts/download-offline-tafsir.mjs",
    "offline:download": "npm run quran-text:download && npm run tafsir:download && npm run hadith:download",
    "offline:check": "node scripts/check-offline-data.mjs",
    "hadith:download": "node scripts/download-offline-hadith.mjs",
    "android:check-assets": "node scripts/check-android-mushaf-assets.mjs && node scripts/check-android-offline-assets.mjs",
    "release:check": "node scripts/check-release-polish.mjs"
  },
  "dependencies": {
    "lucide-react": "latest",
    "next": "16.2.6",
    "react": "19.2.0",
    "react-dom": "19.2.0",
    "@supabase/supabase-js": "^2.46.1",
    "@capacitor/core": "latest",
    "@capacitor/app": "latest",
    "@capacitor/local-notifications": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "autoprefixer": "10.4.20",
    "eslint": "latest",
    "eslint-config-next": "latest",
    "postcss": "8.4.49",
    "tailwindcss": "3.4.17",
    "typescript": "latest",
    "@capacitor/cli": "latest",
    "@capacitor/android": "latest",
    "@capacitor/assets": "latest"
  }
}
